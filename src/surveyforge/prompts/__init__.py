"""Prompt Contract Pack (per spec § 2.6).

Per-role Markdown prompts with YAML front-matter declaring role / version /
schema / allowed_tools / forbidden. Loaded and validated by `PromptRegistry`
(see `loader.py`).
"""
